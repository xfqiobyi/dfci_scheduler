logging:
  level: INFO
  loggers:
    com.dfci.scheduler: DEBUG
    org.hibernate.engine: WARN
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout

database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:./target/scheduler
